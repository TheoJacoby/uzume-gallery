{% extends 'base.html.twig' %}

{% block title %}Administration - Peintures{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Gestion des Peintures</h1>
        <a href="{{ path('admin_painting_new') }}" class="btn btn-success">Nouvelle peinture</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Titre</th>
                    <th>Auteur</th>
                    <th>Catégorie</th>
                    <th>Date</th>
                    <th>Publié</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for painting in paintings %}
                    <tr>
                        <td>
                            {% if painting.imageName %}
                                <img src="{{ vich_uploader_asset(painting, 'imageFile') }}" alt="{{ painting.title }}" style="width: 80px; height: 80px; object-fit: cover;" class="rounded">
                            {% else %}
                                <img src="{{ asset('images/paintings/1.jpeg') }}" alt="Image par défaut" style="width: 80px; height: 80px; object-fit: cover;" class="rounded">
                            {% endif %}
                        </td>
                        <td>{{ painting.title }}</td>
                        <td>
                            {% if painting.user %}
                                <div class="d-flex align-items-center">
                                    {% if painting.user.imageName %}
                                        <img src="{{ vich_uploader_asset(painting.user, 'imageFile') }}" alt="{{ painting.user.getFullName() }}" style="width: 30px; height: 30px; border-radius: 50%; object-fit: cover; margin-right: 0.5rem;">
                                    {% else %}
                                        <div style="width: 30px; height: 30px; border-radius: 50%; background: var(--japanese-red); color: white; display: flex; align-items: center; justify-content: center; margin-right: 0.5rem; font-size: 0.7rem; font-weight: bold;">
                                            {{ painting.user.firstName|first }}{{ painting.user.lastName|first }}
                                        </div>
                                    {% endif %}
                                    <span>{{ painting.user.getFullName() }}</span>
                                </div>
                            {% else %}
                                <span class="text-muted">Non assigné</span>
                            {% endif %}
                        </td>
                        <td>{{ painting.category.name }}</td>
                        <td>{{ painting.created|date('d/m/Y') }}</td>
                        <td>
                            <form method="post" action="{{ path('admin_painting_toggle_publish', {id: painting.id}) }}" class="d-inline">
                                <input type="hidden" name="_token" value="{{ csrf_token('toggle' ~ painting.id) }}">
                                <button type="submit" class="btn btn-sm btn-link p-0">
                                    {% if painting.isPublished %}
                                        <i class="bi bi-eye-fill text-success" style="font-size: 1.5rem;">👁️</i>
                                    {% else %}
                                        <i class="bi bi-eye-slash-fill text-secondary" style="font-size: 1.5rem;">👁️‍🗨️</i>
                                    {% endif %}
                                </button>
                            </form>
                        </td>
                        <td>
                            <div class="btn-group" role="group">
                                <a href="{{ path('admin_painting_show', {id: painting.id}) }}" class="btn btn-sm btn-info">Voir</a>
                                <a href="{{ path('admin_painting_edit', {id: painting.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
                                <form method="post" action="{{ path('admin_painting_delete', {id: painting.id}) }}" class="d-inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette peinture ?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ painting.id) }}">
                                    <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="text-center">Aucune peinture trouvée</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="mt-3">
        <a href="{{ path('admin_comment_index') }}" class="btn btn-secondary">Gérer les commentaires</a>
    </div>
{% endblock %}


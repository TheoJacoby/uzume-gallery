{% extends 'base.html.twig' %}

{% block title %}Erreur {{ status_code }} - Uzume Gallery{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center align-items-center" style="min-height: 70vh;">
            <div class="col-md-8 col-lg-6 text-center">
                <div class="card" style="border-radius: 20px; box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15); border: none; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);">
                    <div class="card-body p-5">
                        {# Icône ou emoji #}
                        <div style="font-size: 6rem; margin-bottom: 2rem;">
                            {% if status_code == 404 %}
                                🔍
                            {% elseif status_code == 500 %}
                                ⚠️
                            {% else %}
                                ❌
                            {% endif %}
                        </div>
                        
                        <h1 class="mb-4" style="color: var(--japanese-red); font-weight: 800; font-size: 2.5rem;">
                            Erreur {{ status_code }}
                        </h1>
                        
                        <p class="lead mb-4" style="color: #4a5568; line-height: 1.8;">
                            {{ status_text }}
                        </p>
                        
                        <div class="d-flex flex-column flex-md-row gap-3 justify-content-center">
                            <a href="{{ path('app_home') }}" class="btn btn-primary" style="background: linear-gradient(135deg, var(--japanese-red) 0%, var(--japanese-red-dark) 100%); border: none; border-radius: 20px; padding: 0.75rem 2rem; font-weight: 600; box-shadow: 0 4px 15px rgba(220, 20, 60, 0.3); transition: all 0.3s ease;">
                                🏠 Retour à l'accueil
                            </a>
                            <a href="{{ path('app_gallery') }}" class="btn btn-outline-primary" style="border-color: var(--japanese-red); color: var(--japanese-red); border-radius: 20px; padding: 0.75rem 2rem; font-weight: 600; transition: all 0.3s ease;">
                                🎨 Voir la galerie
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <style>
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(220, 20, 60, 0.4) !important;
        }
        
        .btn-outline-primary:hover {
            transform: translateY(-2px);
            background: var(--japanese-red) !important;
            color: white !important;
            border-color: var(--japanese-red) !important;
        }
    </style>
{% endblock %}

